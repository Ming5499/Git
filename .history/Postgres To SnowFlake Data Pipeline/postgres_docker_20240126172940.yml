version: '3.1'

service:
  postgres:
    image: postgres:lastest
    container_name: postgres
    restart: always
    enviroment:
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
    volumns:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata: